const CACHE_NAME = 'alkohol-tracker-v1.2.0';
const ASSETS = [ './', './index.html', './style.css', './app.js', './manifest.json', './icons/icon-192.png', './icons/icon-512.png', 'https://cdn.jsdelivr.net/npm/chart.js@4.4.1/dist/chart.umd.min.js' ];
self.addEventListener('install', (event) => { event.waitUntil(caches.open(CACHE_NAME).then((cache) => cache.addAll(ASSETS))); });
self.addEventListener('activate', (event) => { event.waitUntil(caches.keys().then(keys => Promise.all(keys.filter(k => k !== CACHE_NAME).map(k => caches.delete(k))))); });
self.addEventListener('fetch', (event) => { const { request } = event; if (ASSETS.includes(request.url) || ASSETS.some(a => request.url.endsWith(a.replace('./','')))) { event.respondWith(caches.match(request).then((resp) => resp || fetch(request).then((net) => { const clone = net.clone(); caches.open(CACHE_NAME).then((c) => c.put(request, clone)); return net; }))); return; } event.respondWith(fetch(request).then((resp) => { const clone = resp.clone(); caches.open(CACHE_NAME).then((c) => c.put(request, clone)); return resp; }).catch(() => caches.match(request))); });
